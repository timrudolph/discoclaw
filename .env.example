# ============================================================
# Discoclaw — Quick Start Configuration
# ============================================================
# Copy this file to .env and fill in the two REQUIRED values.
# Or run `pnpm setup` for guided interactive configuration.
# For all ~100 options, see .env.example.full
# ============================================================

# ----------------------------------------------------------
# REQUIRED — the bot won't start without these
# ----------------------------------------------------------

# Your Discord bot token (from https://discord.com/developers/applications)
# Important: Enable Message Content Intent in Developer Portal
# (Bot → Privileged Gateway Intents) or the bot receives empty messages.
DISCORD_TOKEN=

# Comma-separated Discord user IDs allowed to talk to the bot.
# Empty = nobody can use it (fail-closed).
DISCORD_ALLOW_USER_IDS=

# ----------------------------------------------------------
# COMPONENTS — choose what to run (default: discord only)
# ----------------------------------------------------------

# Enable the Discord bot (default: 1)
#DISCOCLAW_DISCORD_ENABLED=1

# Enable the native client server for the iOS/macOS app (default: 0)
#DISCOCLAW_SERVER_ENABLED=0

# Beads/backlog feature — set both to 0 to disable across Discord bot, server API, and client UI
#DISCOCLAW_BEADS_ENABLED=1
#SERVER_BEADS_ENABLED=1

# ----------------------------------------------------------
# CORE — most users will want to review these
# ----------------------------------------------------------

# Guild (server) ID — used for bead hooks and system channel bootstrap.
DISCORD_GUILD_ID=

# Where the Claude CLI runs (its working directory).
# Default: ./workspace (or $DISCOCLAW_DATA_DIR/workspace if DATA_DIR is set)
#WORKSPACE_CWD=

# Which Claude model to use.
#RUNTIME_MODEL=opus

# Output format for the Claude CLI. stream-json gives smoother streaming.
#CLAUDE_OUTPUT_FORMAT=stream-json

# Allow Claude to run without permission prompts. This is what makes
# headless/Discord operation possible — the Discord allowlist above
# is the security boundary instead.
#CLAUDE_DANGEROUSLY_SKIP_PERMISSIONS=1

# Log level: trace | debug | info | warn | error | fatal
#LOG_LEVEL=info

# ----------------------------------------------------------
# NATIVE CLIENT SERVER — only needed if using the iOS/macOS app
# ----------------------------------------------------------

# Registration token — clients must present this to pair with the server.
# Generate with: openssl rand -hex 32
#SETUP_TOKEN=

# Port the API listens on (default: 4242)
#SERVER_PORT=4242

# Bind interface — 0.0.0.0 exposes on LAN (required for iOS/remote devices)
#SERVER_HOST=0.0.0.0

# TLS (both required together for HTTPS — strongly recommended on LAN)
#TLS_CERT=/path/to/cert.pem
#TLS_KEY=/path/to/key.pem

# ----------------------------------------------------------
# For all ~90 options (subsystems, actions, memory, identity,
# observability, advanced/debug), see .env.example.full
# ----------------------------------------------------------
